{
  "schema_version": "apf.resource-limits/v2",
  "accumulation_mode": "bounded",
  "max_platforms_per_run": 8,
  "max_files_per_platform": 5000,
  "max_inbox_packets": 32,
  "max_seconds_per_platform": 120,
  "max_memory_mb_per_platform": 512,
  "sequential_platform_analysis": true,
  "parallel_platform_workers": 1,
  "notes": [
    "Bounded defaults preserve mailbox backpressure and predictable owner-PC load.",
    "Unbounded collection is not permitted as a default runtime policy.",
    "Limits can be raised only through an owner-reviewed policy change."
  ]
}
