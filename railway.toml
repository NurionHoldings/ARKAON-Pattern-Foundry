[build]
builder = "RAILPACK"
buildCommand = "pip install -e ."

[deploy]
startCommand = "python -m uvicorn apf.api:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
